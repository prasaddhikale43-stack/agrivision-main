rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow users to upload activity images to their own folder.
    // The path is matched against the uploaded file's full path.
    match /activities/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
